root = true // 初めの行に記述する。この記述をすることで、フォルダ以下のディレクトリのみにルールが適用される

[*] // すべてのファイルに適用する
charset = utf-8 // 文字コードを統一
trim_trailing_whitespace = true // 行末のホワイトスペースを削除
insert_final_newline = false // フォルダの最後の行に改行
end_of_line = crlf // 改行コードを指定する
